<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CoffeeShop" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brown</item>
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="Theme.CoffeeShop" parent="Base.Theme.CoffeeShop" />

    <style name="Theme.CoffeeShop.Main" parent="Base.Theme.CoffeeShop">
        <item name="android:statusBarColor">@color/dark</item>
    </style>

    <style name="Theme.CoffeeShop.ItemSize">
        <item name="backgroundTint">@color/item_size_enabled</item>
    </style>

</resources>